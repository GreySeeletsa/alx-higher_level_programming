#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    n_dictionary = a_dictionary.copy()
    k_list = list(n_dictionary.keys())
    for x in k_list:
        n_dirctionary[x] *= 2
        return (n_dictionary)
