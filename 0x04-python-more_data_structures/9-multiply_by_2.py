#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    n_directionary = a_dictionary.copy()
    k_list = list(n_direction.keys())
    for x in k_list:
        n_directionary[x] *= 2
        return (n_directionary)
